@model int

@{
    var db = new NewsApplication.Infrastructure.ApplicationDBContext();        
    var list = new SelectList(db.States.ToList(), "ID", "Name", Model);
}

@Html.DropDownListFor(m => m, list)
